---
title: Installation
format:
  html:
    toc: true
    toc-depth: 3
    number-sections: true
---

::: {.callout-note}
## Useful links (IBM Docs)

- [Installing Quantum Safe Explorer on Windows](https://www.ibm.com/docs/en/quantum-safe/quantum-safe-explorer/2.x_beta?topic=explorer-installing-quantum-safe-windows)
- [Installing Quantum Safe Explorer on macOS](https://www.ibm.com/docs/en/quantum-safe/quantum-safe-explorer/2.x_beta?topic=explorer-installing-quantum-safe-macos)
- [Uninstalling Quantum Safe Explorer on Windows](https://www.ibm.com/docs/en/quantum-safe/quantum-safe-explorer/2.x_beta?topic=explorer-uninstalling-quantum-safe-from-windows)
- [Uninstalling Quantum Safe Explorer on macOS](https://www.ibm.com/docs/en/quantum-safe/quantum-safe-explorer/2.x_beta?topic=explorer-uninstalling-quantum-safe-from-macos)
- [Installing/Uninstalling QSE VS Code extension](https://www.ibm.com/docs/en/quantum-safe/quantum-safe-explorer/2.x_beta?topic=installing-uninstalling-quantum-safe-explorer-visual-studio-code-extension)
- [Configuring QSE VS Code extension](https://www.ibm.com/docs/en/quantum-safe/quantum-safe-explorer/2.x_beta?topic=configuring-quantum-safe-explorer-visual-studio-code-extension)

> Tip: Use the top-left **Change version** dropdown to select the version you’re running.
:::

# Overview

This guide provides step-by-step instructions for downloading, installing, configuring, and running **IBM Quantum Safe Explorer (QSE)**.

It covers:

- Prerequisites (VS Code + Java)
- Where to download QSE packages
- Installing the QSE service (Windows/macOS)
- Installing the VS Code extension (`.vsix`)
- Post-install configuration in VS Code
- Running scans
- Troubleshooting

# Prerequisites

For any installation, to use the VS Code plugin you must install **VS Code** and **OpenJDK 17+**.

**Supported / recommended environment**

- OS: Windows 11 or macOS (Ventura on Intel, M1 or higher)
- RAM: 16 GB minimum, 32 GB recommended
- OpenJDK 17.0.0 or higher
- Git Bash (Windows)

## Install Visual Studio Code

::: {.panel-tabset}

### Windows

Grab VS Code for Windows from [https://code.visualstudio.com/download](https://code.visualstudio.com/download)

- Download the **System Installer**.

![Windows1](/images/windows-installer01.png)

Double-click and install to the following path (as shown below):

![Windows2](/images/windows-installer02.png)

![Windows3](/images/windows-installer03.png)

### macOS

Grab VS Code for macOS from [https://code.visualstudio.com/download](https://code.visualstudio.com/download)

- Download the **.dmg file** for your architecture (for M1 and newer, choose *Apple silicon*)

![MacOS1](/images/macos-installer01.png)

- Open the downloaded file (double-click)

- Drag Visual Studio Code.app to the Applications folder

:::

## Install OpenJDK 17+

::: {.panel-tabset}

### Windows

Microsoft's OpenJDK 17 (minimum version) can be found here:

- [https://learn.microsoft.com/en-us/java/openjdk/download#openjdk-17](https://learn.microsoft.com/en-us/java/openjdk/download#openjdk-17)

To see all versions:

- [https://learn.microsoft.com/en-us/java/openjdk/download](https://learn.microsoft.com/en-us/java/openjdk/download)

Go to your `Downloads` folder and double-click the `microsoft-jdk-17.0.11-windows-x64.msi` file.

- Let the installer set `JAVA_HOME`.
- Click `Next`, then `Install`.

![WindowsJDK](/images/windows-jdk01.png)

### macOS

Install OpenJDK 17 on Homebrew 

> Refer to: [https://formulae.brew.sh/formula/openjdk](https://formulae.brew.sh/formula/openjdk)

```bash
brew install openjdk@17
```

For the latest version, simply

```bash
brew install openjdk
```

If you have other Java versions installed, you may need to add OpenJDK 17 (replace *17* with your version) to your PATH:

```bash
echo 'export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"' >> ~/.bashrc

. ~/.bashrc
```

:::

# Download required software

## Navigate to the software downloads page

Choose the appropriate download site and **accept all license and usage agreements**:

- **IBMers:** <https://webxlsdf.webui-dswdown.dal.app.cirrus.ibm.com/>
- **Clients:** <https://www.ibm.com/software/passportadvantage/pao-customer>

## Download platform-specific bundles

Download the application bundles corresponding to your operating system.

> Note: In many distributions, the QSE artifacts come as multiple zip packages that must be extracted into the **same folder**.

## Download the installer (optional)

Download the QSE installer package if you prefer installing via PKG (macOS) / EXE (Windows) rather than manual setup.

# Extract the QSE packages

After download, extract/unzip **all QSE artifacts into the same directory**.

> Important: These files often use the same base name; keep them together.

::: {.panel-tabset}

## Windows

![QSE2](/images/windows-qse1.png)

Extract them all to the same folder:

![QSE3](/images/windows-qse2.png)

Example folder listing:

```powershell
PS C:\Users\Administrator\Downloads\IIQSE0_1.0.1_MP_EN> tree /F
Folder PATH listing
Volume serial number is 1EA9-8239
C:.
├───M0GT5EN
│       ibm-quantum-safe-explorer-1.0.1.vsix
│
├───M0GT6EN
│   │   cli.sh
│   │   LicenseAcceptance.config
│   │   version
│   │
│   ├───la_home
│   │       LA_cs
│   │       ...
│   ├───lib
│   │       ...
│   └───swidtag
│           ibm.com_IBM_Quantum_Safe_Explorer-1.0.1.swidtag
│
└───M0GT7EN
        IBM Quantum Safe Explorer.exe
```

## macOS

Example artifacts:

```bash
-rw-r--r--@ 1 user staff  35M Jun 26 15:59 IIQSE0_1.0.1_MP_EN(2).zip
-rw-r--r--@ 1 user staff  73M Jun 26 15:58 IIQSE0_1.0.1_MP_EN.zip
-rw-r--r--@ 1 user staff 446K Jun 26 15:58 IIQSE0_1.0.1_MP_EN(1).zip
```

Unzip them all into the same folder:

![QSE2](/images/macos-qse1.png)

Example directory structure:

```bash
tree
.
├── M0Z1CEN
│   └── IBM Quantum Safe Explorer.pkg
├── M0Z1FEN
│   └── ibm-quantum-safe-explorer-2.2.5-0.vsix
└── M0Z1BEN
    └──CLI
        ├── LicenseAcceptance.config
        ├── cli.sh
        ├── version
        ├── la_home
        ├── lib
        ├── swidtag
        └── insights
```

:::

# Install QSE service (application)

This installs and runs the local QSE service that the VS Code extension and CLI talk to.

::: {.panel-tabset}

## Windows

Run the installer:

```powershell
PS C:\Users\Administrator\Downloads\IIQSE0_1.0.1_MP_EN> cd .\M0GT7EN\
PS C:\Users\Administrator\Downloads\IIQSE0_1.0.1_MP_EN\M0GT7EN> & '.\IBM Quantum Safe Explorer.exe'
```

The following window will pop up. Use the defaults.

![QSEwindows1](/images/windows-qse-service1.png)

When installation is complete, click **Finish** and allow it to start the service.

![QSEwindows2](/images/windows-qse-service2.png)

Click **Accept** when prompted.

![QSEwindows3](/images/windows-qse-service3.png)

Once it’s running, **do not close the window**.

![QSEwindows4](/images/windows-qse-service4.png)

## macOS

Install the PKG (requires admin):

- Double-click the IBM Quantum Safe Explorer.pkg file

- *Alternatively*, run:
```bash
sudo installer -pkg M0Z1CEN/IBM\ Quantum\ Safe\ Explorer.pkg -target /Applications
```

Locate the desktop shortcut for **IBM Quantum Safe Explorer** and start it.

![QSEmacos1](/images/macos-qse-service1.png)

You should see the following windows; click **Accept**.


![QSEmacos2](/images/macos-qse-service2.png)

The QSE service should now be running.

![QSEmacos3](/images/macos-qse-service3.png)

::: {.callout-warning}
Do not close this window. In some versions it does **not** continue running in the background. Minimize if you wish to dismiss it.
:::

:::

# Install the CLI (optional but useful)

The `cli.sh` script can be used to run scans from a terminal and is also used by some workflows.

::: {.panel-tabset}

## Windows

Simply change directory to where you extracted the CLI bundle. The `cli.sh` file will be used to perform the actual scan. Assuming you unzipped from your `\Downloads` directory (example):

```powershell
PS C:\Users\Administrator\Downloads\IIQSE0_1.0.1_MP_EN> cd .\M0GT6EN\
```

## macOS

Simply change directory to where you extracted the CLI bundle. The `cli.sh` file will be used to perform the actual scan. Assuming you unzipped from your `\Downloads` directory (example):

```bash
cd ~/Downloads/M0Z1BEN/CLI
```

:::

# Install the VS Code extension (.vsix)

The VS Code extension ships as a `.vsix` file (example: `quantum-safe-explorer-2.2.5-0.vsix`).

## Install using the VS Code CLI

::: {.panel-tabset}

## Windows

```powershell
PS C:\Users\Administrator\Downloads\IIQSE0_1.0.1_MP_EN> cd .\M0GT5EN\
PS C:\Users\Administrator\Downloads\IIQSE0_1.0.1_MP_EN\M0GT5EN> code --install-extension ibm-quantum-safe-explorer-1.0.1.vsix
Installing extensions...
Extension 'ibm-quantum-safe-explorer-1.0.1.vsix' was successfully installed.
```

## macOS

```bash
cd M0Z1FEN # or whatever this folder is called for you!
code --install-extension quantum-safe-explorer-2.2.5-0.vsix

Installing extensions...
Extension 'quantum-safe-explorer-2.2.5-0.vsix' was successfully installed.
```

:::

## Enable the `code` command (if not installed)

If `code` isn’t recognized:

1. Open **Visual Studio Code**
2. Open the **Command Palette**
   - Windows/Linux: `Ctrl + Shift + P`
   - macOS: `Cmd + Shift + P`
3. Run: **Shell Command: Install 'code' command in PATH**
4. Approve the installation (may require admin password)
5. Restart your terminal and retry:

```bash
code --install-extension quantum-safe-explorer-2.2.5-0.vsix
```

# Configure the VS Code extension

In VS Code:

1. Click the **Extensions** icon (left sidebar)
2. Select **IBM Quantum Safe Explorer**

![Extension1](/images/extension1.png)

3. Click the extension **Settings**

![Extension2](/images/extension2.png)

## Required settings

- Click **Edit in settings.json**:

![Extension3](/images/extension3.png)

- **qs-explorer.sourceFileExtensions** — Source file types to scan (e.g., `.java`, `.py`)

```json
    "qs-explorer.sourceFileExtensions": [
        ".java",
        ".jar",
        ".dart",
        ".go",
        ".cpp",
        ".cs",
        ".py"
    ],
```

## Additional configuration (paths/credentials/workspace)

Some environments require further setup (paths, credentials, workspace settings, or additional tooling). See:

- <https://www.ibm.com/docs/en/quantum-safe/quantum-safe-explorer/2.x_beta?topic=configuring-quantum-safe-explorer-visual-studio-code-extension>

# Run a Quantum Safe scan (VS Code)

Once installed and configured:

1. Open VS Code
2. Open a project or files to scan
3. Open the Command Palette:
   - Windows/Linux: `Ctrl + Shift + P`
   - macOS: `Cmd + Shift + P`
4. Run one of:
   - **Quantum Safe Explorer: Scan API Discovery**
   - **Quantum Safe Explorer: Scan Cryptography Analysis**

# Troubleshooting

## “Scan Cryptography Analysis” shows no vulnerabilities / algorithms

If scanning Java source, `.java` files often need to be compiled into `.class` files to appear in the analysis.

```bash
javac your-file.java
```

## Scan results are not updating

QSE uses `qs_explorer_result` and `qs_scan_result` as a cache.

- Delete those folders/files
- Re-run the scan command to get a fresh scan

# Uninstalling the QSE service

::: {.callout-warning}
There is a bug in **v1.0.1** uninstall on Windows that can cause issues when **v2.x** is installed later.
:::

## Windows notes (v1.0.1)

1. Uninstall via **Add or Remove Programs** (normal uninstall)
2. Manually delete the `qs-explorer` folder from the user’s home directory

If the folder is not removed and v2.x is later installed, scanning can fail.

> This issue is resolved in v2.x uninstallers.